import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#ff0000',
    },
    text: {
      main: '#000000',
      white: '#fffff',
    },
  },
});

export default theme;
